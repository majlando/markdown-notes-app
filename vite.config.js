import { defineConfig } from 'vite';

export default defineConfig({
  // We can add plugins or other Vite options here later if needed
  // For a basic setup, the default configuration is often sufficient.
  // If your main HTML file is not index.html in the root, specify it:
  // root: '.', 
  // build: {
  //   outDir: 'dist'
  // }
});
